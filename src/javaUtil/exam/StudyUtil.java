package javaUtil.exam;

public class StudyUtil {
	
	//java.util은 거의 두번째 로 많이 사용하는 것.
	// 자료구조 관련 ,  컬렉션 프레임워크 인터페이스등 , 날짜,date 등
	//calendar , list set collection map 등도 
	// java api에서 확인 가능  java util 패키지 안에서 확인
	
	
	// 컬렉션 프레임 워크 - 자료구조 프레임워크
	// Collection interface - 여기에 자료가 있다 하고 표현
	// 중복허용, 순서는 기억못함 
	
	// add(Object) : boolean 추가, iterator() : iterator 자료꺼내기, size() : int 자료수 등
	// 순서를 기억 못하기때문에 Iterator interface 의존
	// hasNext() :boolean 꺼낼꺼 있는지 확인, next(): Object 꺼낼때 씀
	
	// Set interface - Collection interface 상속 
	// add(Object) : boolean 같은자료형 있으면 false 있으면 true
	
	// List interface- 순서기억
	// get(int) :Object - 번호 , 
	
	// Map interface - - key, value 쌍으로 key값을 넣으면 value 찾을 수 있게 구상 , key는 중복안됨
	// get(Object) : Object - key 넣으면 value 반환, keySet() : Set  - map 에 있는 키를 다 읽어올 수 있음,
	// Put(Object, Object) : void - key, value 넣는것
	
	// 밑에는 Arraylist, hashSet, hashMap 등이 있다.
	
	
	
	
	
	
	
}
